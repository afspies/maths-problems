# {{TITLE}}

## Statement
{{QUESTION}}

TODO: precise statement, standard formulations, and equivalences.

## Status / context
TODO: history, best known results *with citations* (put refs in literature/refs.bib),
and why this problem fits the certificate-plus-verifier regime.

## Certificate + verifier
- **Certificate**: TODO — the exact finite object that would settle the
  interesting direction.
- **Verifier**: TODO — the exact-arithmetic check, expected cost, and what tool
  implements it (goes in harness/, built and validated on a known object FIRST).
- Note any direction that has *no* cheap certificate — be explicit about what
  cannot be claimed from search alone.

## Known structure (bake into any search)
TODO: constraints, no-go theorems, symmetry restrictions, parameter conditions —
everything that prunes the space or shapes encodings.

## Angle-of-attack menu (be exploratory — draw from different fields)
- TODO: 3–6 angles from genuinely different branches (algebraic construction,
  SAT/constraint, spectral/SDP relaxation, probabilistic, geometric, ...).
  Each attempted angle gets its own angles/<name>/ subfolder with a README.

## First steps
1. Build + validate the verifier on a known object.
2. Literature map with citations into literature/.
3. TODO: the cheapest informative experiment.
