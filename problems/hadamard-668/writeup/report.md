# Hadamard matrix of order 668: results

**Status of this write-up:** draft · **Problem:** Construct a 668x668 +-1 matrix H with H H^T = 668 I (smallest open order).

## Abstract

One paragraph: what was established, how it was verified, what remains open.
Be precise about the epistemic status — "verified certificate", "exhaustive
search of subspace X", "heuristic evidence" are different claims.

## Result

The exact statement(s), with pointers to `../certificates/` or `../results/`
artifacts and the verifier that checks them.

## Verification

How to reproduce the check from a clean clone, exact commands, expected output,
and runtime. A reader must be able to verify the headline claim in minutes.

## Method

What was searched, how the space was structured/pruned, what tooling ran where
(describe compute generically), and which model-guided steps mattered.

## What we tried that didn't work

Summarize the dead ends and why they died (mirrors ../LEARNINGS.md) — this is a
first-class section, not an appendix.

## Relation to prior work

Citations into ../literature/refs.bib.

## Cite as

See CITATION.cff in this folder (DOI is added by tools/release.py on release).
