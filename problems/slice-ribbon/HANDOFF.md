# Handoff prompt — Slice-ribbon and exotic S4 candidates

Keep this current: every session updates it before finishing, so the next
session's chip/prompt encodes the latest LEARNINGS. This file IS the prompt —
paste it (or point a session at it) to continue work on this problem.

---

You are working on: Slice-ribbon and exotic S4 candidates — Is every smoothly slice knot ribbon? Attack via obstruction computations on standing candidate families (GST knots, Gluck twists)

Work in problems/slice-ribbon/ of the maths-problems repo. Read, in order:
1. /AGENTS.md (repo root) — session conventions: branch `problem/slice-ribbon/<date>-<topic>`,
   stay in your subfolder, exact-arithmetic verification, Codex second opinions
   (GPT-5.6 Sol, xhigh), JOURNAL/LEARNINGS/STATUS updates, board regeneration,
   publishing and DOI rules. If AGENTS.local.md exists, follow it too.
2. problems/slice-ribbon/PROBLEM.md — statement, certificate/verifier spec, known
   structure, attack-angle menu.
3. problems/slice-ribbon/LEARNINGS.md — do what "next session should do first" says.
4. problems/slice-ribbon/JOURNAL.md — recent sessions' detail, if needed.

Current priorities (update each session):
- Build/validate the verifier on a known object; then first angle from PROBLEM.md.
