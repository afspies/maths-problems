# Attack angles

One subfolder per angle (e.g. `sat-prescribed-symmetry/`, `algebraic-construction/`,
`sdp-relaxation/`). Each angle's README states: the idea, its mathematical origin,
current status (untried / active / dead / promising), and the verdict with a
pointer to journal entries. Rotate deliberately — don't tunnel on the first
encoding that runs.
