# Harness

Shared code for this problem: the verifier (built FIRST, validated on a known
object before any search), encoders, and search utilities. Exact arithmetic
only in anything verification-adjacent — floats may steer heuristics, never
certify claims.
