# Certificates

Verified positive objects only. Each certificate ships as: the object (data
file), a `verify_*.py` script (or equivalent) that checks it from scratch in
exact arithmetic, and the expected output. Nothing lands here without the
verifier passing.
